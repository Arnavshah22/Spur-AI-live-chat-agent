# Keep this aligned with the backend MAX_MESSAGE_LENGTH value.
VITE_MAX_MESSAGE_LENGTH=2000

# Optional when the API is deployed on a different origin.
# VITE_API_BASE_URL=https://your-api.example.com/api/chat
